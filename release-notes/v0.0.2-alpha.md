## What's New

- Added curl one-liner installer to README
- Installer now supports `--prerelease` flag to install pre-release versions

## Bug Fixes

- Fixed release archive structure to use nested directory (standard convention)
- Fixed installer to properly handle pre-release versions from GitHub API

## Breaking Changes

- None
