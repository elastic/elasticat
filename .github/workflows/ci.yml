name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libgl1-mesa-dev libxxf86vm-dev

      - name: Run tests
        run: go test ./...

  checks:
    name: Lint & Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Check formatting
        run: make fmt-check

      - name: Check license headers
        run: make license-check

      - name: Regenerate NOTICE.txt
        run: make notice

      - name: Check NOTICE.txt is up-to-date
        run: |
          if ! git diff --exit-code NOTICE.txt; then
            echo ""
            echo "ERROR: NOTICE.txt is out of date!"
            echo "Please run 'make prep' locally and commit the changes"
            exit 1
          fi
          echo "NOTICE.txt is up-to-date"

  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            archive_dir: elasticat-linux-amd64
            elasticat_binary: elasticat-linux-amd64
            catseye_binary: catseye-linux-amd64
            archive_ext: tar.gz
          - os: macos-latest
            goos: darwin
            goarch: arm64
            archive_dir: elasticat-darwin-arm64
            elasticat_binary: elasticat-darwin-arm64
            catseye_binary: catseye-darwin-arm64
            archive_ext: tar.gz
          - os: windows-latest
            goos: windows
            goarch: amd64
            archive_dir: elasticat-windows-amd64
            elasticat_binary: elasticat-windows-amd64.exe
            catseye_binary: catseye-windows-amd64.exe
            archive_ext: zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libgl1-mesa-dev libxxf86vm-dev

      - name: Build binaries and create archive (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          mkdir -p dist/${{ matrix.archive_dir }}
          go build -o dist/${{ matrix.archive_dir }}/${{ matrix.elasticat_binary }} ./cmd/elasticat
          go build -o dist/${{ matrix.archive_dir }}/${{ matrix.catseye_binary }} ./cmd/catseye
          cp LICENSE.txt NOTICE.txt README.md dist/${{ matrix.archive_dir }}/
          cd dist
          tar -czvf ${{ matrix.archive_dir }}.tar.gz ${{ matrix.archive_dir }}
        shell: bash

      - name: Build binaries and create archive (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          New-Item -ItemType Directory -Force -Path dist/${{ matrix.archive_dir }}
          go build -o dist/${{ matrix.archive_dir }}/${{ matrix.elasticat_binary }} ./cmd/elasticat
          go build -o dist/${{ matrix.archive_dir }}/${{ matrix.catseye_binary }} ./cmd/catseye
          Copy-Item LICENSE.txt, NOTICE.txt, README.md -Destination dist/${{ matrix.archive_dir }}/
          Compress-Archive -Path dist/${{ matrix.archive_dir }} -DestinationPath dist/${{ matrix.archive_dir }}.zip
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive_dir }}.${{ matrix.archive_ext }}
          path: dist/${{ matrix.archive_dir }}.${{ matrix.archive_ext }}
          retention-days: 7

