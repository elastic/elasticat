name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libgl1-mesa-dev libxxf86vm-dev

      - name: Run tests
        run: go test ./...

  checks:
    name: Lint & Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Check formatting
        run: make fmt-check

      - name: Check license headers
        run: make license-check

      - name: Regenerate NOTICE.txt
        run: make notice

      - name: Check NOTICE.txt is up-to-date
        run: |
          if ! git diff --exit-code NOTICE.txt; then
            echo ""
            echo "ERROR: NOTICE.txt is out of date!"
            echo "Please run 'make prep' locally and commit the changes"
            exit 1
          fi
          echo "NOTICE.txt is up-to-date"

  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            binary: elasticat-linux-amd64
            archive_ext: tar.gz
          - os: macos-latest
            goos: darwin
            goarch: arm64
            binary: elasticat-darwin-arm64
            archive_ext: tar.gz
          - os: windows-latest
            goos: windows
            goarch: amd64
            binary: elasticat-windows-amd64.exe
            archive_ext: zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libgl1-mesa-dev libxxf86vm-dev

      - name: Build binary and create archive (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          mkdir -p dist/${{ matrix.binary }}
          go build -o dist/${{ matrix.binary }}/${{ matrix.binary }} ./cmd/elasticat
          cp LICENSE.txt NOTICE.txt README.md dist/${{ matrix.binary }}/
          cd dist
          tar -czvf ${{ matrix.binary }}.tar.gz ${{ matrix.binary }}
        shell: bash

      - name: Build binary and create archive (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          New-Item -ItemType Directory -Force -Path dist/${{ matrix.binary }}
          go build -o dist/${{ matrix.binary }}/${{ matrix.binary }} ./cmd/elasticat
          Copy-Item LICENSE.txt, NOTICE.txt, README.md -Destination dist/${{ matrix.binary }}/
          Compress-Archive -Path dist/${{ matrix.binary }} -DestinationPath dist/${{ matrix.binary }}.zip
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}.${{ matrix.archive_ext }}
          path: dist/${{ matrix.binary }}.${{ matrix.archive_ext }}
          retention-days: 7

